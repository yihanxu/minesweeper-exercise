// Dynamic code version 3, 6 Aug 2018

// generate dynamically coded playboard

const generatePlayBoard (numberOfRows, numberOfColumns){
	let board = [],

	for (i = 0, i < numberOfColumns.length, i ++){
		for (j = 0, j < numberOfRows.length, j++){

		}
	}
};